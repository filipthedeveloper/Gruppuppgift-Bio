@model IEnumerable<Ja.Models.BokadePlatser>

@{
    ViewBag.Title = "Boka";
}

    <link href="~/Content/filminfo.css" rel="stylesheet" />
<h2>Boka</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AntalBokadePlatser)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.VisningsSchemaId)
            </th>*@
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AntalBokadePlatser)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.VisningsSchemaId)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>*@
        </tr>
    }


    <tr>
        <td>
            <form method="post" action="@Url.Action("Boka", "Biljett")">
                <input type="text" id="antalIn" name="antalIn" placeholder="Antal">
                @*<input type="text" id="testar" name="testar" placeholder="test">*@
                @*<input type="number" id="antal" name="antal" placeholder="Antal">*@
                @*@Html.HiddenFor(model => model.VisningsSchemaId, new { id = Model.VisningsSchemaId })Skickar med schema-id vidare, tillsammans med antal platser ovan*@
                <br />
                <input type="submit" value="Bekräfta" />
            </form>
        </td>
    </tr>

</table>
